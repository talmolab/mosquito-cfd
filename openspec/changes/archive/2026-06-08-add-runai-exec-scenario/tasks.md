## Tasks

1. [x] Capture the `runai workspace exec` scenario as a `## MODIFIED Requirements` delta on the Workspace Management Commands requirement
2. [x] `openspec validate add-runai-exec-scenario --strict`
3. [ ] Revert the undocumented direct edit to `openspec/specs/runai-cluster-skill/spec.md` (content now lives in this change's delta)
4. [ ] `openspec archive add-runai-exec-scenario --yes` to apply the delta to the published spec
