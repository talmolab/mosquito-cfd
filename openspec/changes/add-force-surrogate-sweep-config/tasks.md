# Tasks — add-force-surrogate-sweep-config

TDD-ordered: tests (red) → library implementation (green) → generate committed artifacts →
artifact tests (green) → validation. The suite is RED between §1 and §2 (the §1 tests import
`mosquito_cfd.force_surrogate.sweep`, which does not exist yet — a *collection error* (exit 2),
NOT the tolerated exit-5). Do red/green **locally**; land tests + implementation together. Commit
plan in §6.

Every test cites the spec scenario it verifies. All tests are cluster-free (CC-2): they use the
committed `examples/flapping_wing/inputs.3d.validation` (read-only) and `tests/fixtures/micro_sweep.json`
— no RunAI, GPU, or plotfiles. **Hard rule: the §1 tests must NOT reference `examples/prelim_sweep/`**
(that corpus does not exist until §3 / commit 2); only the §4 artifact tests read it.

## 1. Tests (write before implementation — TDD red phase; all land in commit 1)

- [x] 1.1 `test_build_kinematic_grid_default` — no-arg `build_kinematic_grid()` returns 27 configs; `stroke_amp_deg`∈{35,45,55}, `frequency_fstar`∈{0.85,1.0,1.15}, `pitch_amp_deg`∈{30,45,60}; every combination appears exactly once; each dict has exactly the keys `{stroke_amp_deg, frequency_fstar, pitch_amp_deg}`. **Also pin the canonical order**: configs are in `itertools.product(stroke, freq, pitch)` C-order — assert `result[0]`, `result[13]` (center), `result[-1]` against hard-coded expected dicts. (Scenarios: "Default grid cardinality and levels"; "Configs recorded in a canonical, stable order".)
- [x] 1.1b `test_manifest_config_order_canonical` — in a generated sweep, `manifest["configs"][i]["index"] == i` for all i; the order matches `build_kinematic_grid()`; and the per-config `input_file` names, sorted lexicographically, map to the same configuration sequence as the manifest list (filename-glob order ≡ manifest order — the PR3/PR4 handoff guard). (Scenario: "Configs recorded in a canonical, stable order".)
- [x] 1.2 `test_build_kinematic_grid_custom_levels` — passing 2×2×2 custom levels returns 8 configs, every combination once (confirms the builder is a pure function of its inputs, not hard-coded defaults). (Scenario: "Default grid cardinality and levels" — parameterization half.)
- [x] 1.3 `test_generator_consumes_micro_sweep_fixture` — load `tests/fixtures/micro_sweep.json`; pass as `configs=` to `generate_sweep(..., n_holdout=0)` into `tmp_path` → exactly 2 input files written, no schema error; **and** the manifest has 0 holdout / 2 train (`holdout.n_holdout == 0`, `holdout.config_names == []`, every config `split == "train"`) — covers the `n_holdout=0` branch. (Scenarios: "Config schema matches the micro-sweep fixture"; "All configs generated regardless of split"; CC-2 cluster-free guard.)
- [x] 1.4 `test_generate_sweep_rejects_malformed_config` — empty config list raises `ValueError`; a config dict missing `pitch_amp_deg` raises `ValueError` naming the key; a config with an unknown extra key raises. **Atomicity:** on a malformed config, assert `generate_sweep` writes **zero files** to `tmp_path` (validate-before-write, no partial corpus). (Scenario: "Malformed or empty config rejected".)
- [x] 1.5 `test_compute_reynolds_validated_point` — `compute_reynolds(70.0, 1.0, 0.115)` ≈ 100 (`rtol=1e-2`). (Scenario: "Reynolds reproduces the validated point".)
- [x] 1.6 `test_compute_reynolds_uses_midspan_arm` — `compute_reynolds(φ, f*, ν*)` (uses `r_mid=1.5`) equals the same formula with `r_mid=3.0` divided by exactly 2 (`R_TIP/r_mid`). Guards against conflating the normalization arm with the viscous arm. (Scenario: "Reynolds uses the midspan arm, not the tip arm".)
- [x] 1.7 `test_compute_reynolds_rejects_nonpositive_viscosity` — `nu_star=0.0` and `nu_star<0` raise `ValueError`. (Scenario: "Non-positive viscosity rejected".)
- [x] 1.8 `test_derive_run_duration` — `derive_run_duration(0.85, n_wingbeats=2, dt=5e-4)` → `stop_time ≈ 2.3529` (`rtol=1e-6`) and `max_step == 4706`; also `f*=1.0 → stop_time==2.0, max_step==4000`. (Scenario: "Duration matches the per-frequency formula".)
- [x] 1.9 `test_select_holdout_seeded_noncorner` — default grid + fixed seed → `select_holdout` returns exactly 6 indices. Assert the **properties first** (the real guards): `len(set(result)) == 6` (no dups), `set(result).issubset(eligible_noncorner_indices)`, none is a corner, the eligible non-corner set is exactly 19 (8 corners excluded), and same seed → identical set (call twice). **Then** pin the exact index set as a hard-coded regression lock (run-once-and-lock, layered on the property asserts so a logic bug fails a property, not just the pin). (Scenario: "Six non-corner held-out configs, deterministic by seed".)
- [x] 1.10 `test_select_holdout_rejects_too_many` — `select_holdout` with `n_holdout` greater than the eligible count (e.g. the 2-config micro-sweep, or `n_holdout=20` on the grid) raises `ValueError` (no replacement, no infinite loop). (Scenario: "Holdout larger than the eligible set is rejected".)
- [x] 1.11 `test_render_inputs_minimal_diff` — parse base `inputs.3d.validation` and a `render_inputs` output into key→value maps (parser strips inline comments consistently for both); the differing keys are **exactly** `{particle_inputs.kinematics_stroke_amp, particle_inputs.kinematics_frequency, particle_inputs.kinematics_pitch_amp, max_step, stop_time, amr.plot_int}`; assert `geometry_type`, `geometry_file`, `hinge_x/y/z`, `ns.vel_visc_coef`, a projection-tol key byte-unchanged; assert comment + blank lines preserved (line count + a sampled comment line identical). (Scenario: "Generated file differs from base only in the swept and derived keys".)
- [x] 1.12 `test_render_inputs_no_prefix_collision` — the base contains `particle_inputs.kinematics_deviation_amp = 0.0`; assert it is **byte-unchanged** in the rendered output (the rewrite of `..._stroke_amp`/`..._frequency`/`..._pitch_amp` must NOT touch `..._deviation_amp`). Guards exact-key (not prefix) matching. (Scenario: "Generated file differs…" — prefix-sibling half.)
- [x] 1.13 `test_render_inputs_forces_plot_int_minus_one` — `amr.plot_int == -1` in the rendered output even though the base has `100`. (Scenario: "Plot output disabled in every config".)
- [x] 1.14 `test_render_inputs_missing_key_raises` — parametrized over each of the 6 targeted keys removed from the base text → `render_inputs` raises `ValueError` naming the missing key. (Scenario: "Missing target key is rejected".)
- [x] 1.15 `test_render_inputs_formatting` — a representative config renders the exact substrings `particle_inputs.kinematics_frequency = 0.85`, `particle_inputs.kinematics_stroke_amp = 35.0`, `stop_time = 2.3529411764705883`, and `max_step = 4706` (bare integer, no decimal). Pins float→string formatting (`str()`/shortest-repr). (Scenario: "LF newlines and deterministic numeric formatting".)
- [x] 1.15b `test_generated_files_are_lf_on_disk` — generate a sweep into `tmp_path`; read one written input file **and** `sweep_manifest.json` in **binary** (`Path.read_bytes()`) and assert `b"\r" not in data`. This targets the real bug surface — the `open(..., newline="")` **write**, not `render_inputs` string-building — so it catches a Windows `\n`→`\r\n` translation that `.gitattributes` checkout-normalization would otherwise hide. (Scenario: "LF newlines and deterministic numeric formatting".)
- [x] 1.16 `test_generate_sweep_all_configs_and_split` — default `generate_sweep` into `tmp_path` → 27 input files; manifest marks exactly 6 `split=="holdout"` and 21 `"train"`; held-out configs are present as files (label-only, not excluded). **Consistency:** the top-level `holdout.config_names` set equals the set of names of the configs whose per-config `split=="holdout"` (no desync between the two manifest representations). (Scenarios: "All configs generated regardless of split"; split recording.)
- [x] 1.17 `test_generate_sweep_nu_fixed_and_reynolds_recorded` — every generated file parses to `ns.vel_visc_coef == 0.115`; manifest `reynolds_policy=="nu_star_fixed"`, `nu_star==0.115`, and each config's recorded `reynolds == compute_reynolds(stroke, freq, 0.115)` with **exact `==`** (the manifest stores the full-repr float, so this is exact, not `rtol` — a tolerance would hide a truncation bug in the manifest writer). **Absolute format anchor:** assert the on-disk `sweep_manifest.json` *text* contains the exact substrings `"reynolds": 42.5537291206389` (the `s35_f085_p30` config) and `"nu_star": 0.115` (not `0.11500000000000001`), pinning canonical float serialization rather than only self-consistency. (Scenarios: "ν\* identical across all generated files"; "Manifest records the resolved Reynolds policy and per-config Re".)
- [x] 1.18 `test_generate_sweep_duration_per_config` — for every generated file `stop_time * frequency_fstar >= 2` and `ns.fixed_dt == 0.0005`; `max_step`/`stop_time` match `derive_run_duration`. (Scenario: "Every config covers at least the requested whole wingbeats".)
- [x] 1.19 `test_provenance_sidecar_no_digest` — call `generate_sweep(..., timestamp="2020-01-01T00:00:00+00:00")`; read `sweep_provenance.json`: it has `git_commit` (present; tolerate the `{"error":...}`/`None` form defensively), `base_inputs.sha256 == hash_file(inputs.3d.validation)`, `generated_at == "2020-01-01T00:00:00+00:00"`; **no** docker-digest key anywhere in provenance (no value containing `sha256:` other than the base hash context / no `docker_image` key); and assert `git_commit` is **absent** from `sweep_manifest.json` (it lives only in provenance, so the manifest stays byte-reproducible). (Scenario: "Provenance records git + base hash + caller timestamp, and no digest".)
- [x] 1.20 `test_units_sidecar_roundtrips_via_pr1_helper` — `read_units_sidecar(sweep_manifest.units.json)` parses and maps `stroke_amp_deg→"deg"`, `pitch_amp_deg→"deg"`, `frequency_fstar→"dimensionless (f*)"`, `nu_star/reynolds/stop_time→"dimensionless"`. (Scenario: "Units sidecar validates against the dimensionless vocabulary".)
- [x] 1.21 `test_regeneration_byte_identical` — generate into `tmp_path` twice with the **same** seed + timestamp → the 27 input files **and** `sweep_manifest.json` + `sweep_manifest.units.json` are byte-identical across the two runs (core CC-1 guarantee, independent of the committed artifacts). (Scenario: "Byte-identical regeneration".)
- [x] 1.22 Run `uv run pytest tests/test_force_surrogate_sweep.py` — expect FAIL (ImportError on `force_surrogate.sweep`). Confirm RED **locally**.

## 2. Library implementation (TDD green phase; commit 1)

- [x] 2.1 `constants.py` — add `R_MID = 1.5` (midspan viscous arm; unit comment) **and edit the existing `R_TIP=3.0`-vs-`r_mid` comment (lines 13–15) to reference the new `R_MID` symbol** (no parallel prose copy of the same quantity); add `VALIDATED_NU_STAR = 0.115`, `DT = 5e-4`, `N_WINGBEATS = 2`, `N_HOLDOUT = 6`, `HOLDOUT_SEED` (a fixed int), and Aedes level tuples `AEDES_STROKE_AMP_DEG=(35.,45.,55.)`, `AEDES_FREQUENCY_FSTAR=(0.85,1.0,1.15)`, `AEDES_PITCH_AMP_DEG=(30.,45.,60.)`, each source-attributed (Bomphrey 2017). Google-style.
- [x] 2.2 `sweep.py`:
  - `build_kinematic_grid` (pure; validates levels; emits configs in **`itertools.product(stroke, freq, pitch)` C-order**) and a shared config-validator (`ValueError` on empty list / missing / unknown key — D8).
  - `compute_reynolds` (r_mid arm; raises on `nu_star <= 0`).
  - `derive_run_duration` (`stop_time=n/f*`, `max_step=round(stop_time/dt)`).
  - `select_holdout` (`default_rng(seed)` sampling a **sorted** eligible-index list; raises if `n_holdout > eligible`; non-corner eligibility = not all-three-extreme).
  - `render_inputs` (**exact full-key** match — never prefix; preserves comments/order/whitespace/inline comments; raises on missing targeted key; forces `plot_int=-1`; joins with `"\n"`; floats via `str()`/shortest-repr, ints via `str(int)`).
  - `generate_sweep` (orchestrator: **validate configs before writing anything** (atomicity — no partial corpus on a bad config) → build/holdout/duration/reynolds → write inputs with `open(..., "w", encoding="utf-8", newline="")`; file names `inputs.3d.s{φ:d}_f{round(f*×100):03d}_p{α:d}` (**3-digit zero-pad on f** so filename lexsort ≡ config order); write `sweep_manifest.json` (deterministic — NO git/timestamp) with `json.dump(..., sort_keys=True, indent=2, ensure_ascii=False)` and `configs[]` in grid order — `reynolds`/`nu_star` via the default float repr, **never** `round()`/format-spec (canonical, non-truncating); `sweep_manifest.units.json` via `write_units_sidecar`; and a **separate** `sweep_provenance.json` `{tool, generated_at (caller ts), git_commit (get_git_info().get("commit")), base_inputs{path: Path(...).as_posix(), sha256: hash_file(base)}}` — **no** digest; git_commit kept out of the manifest so byte-identity holds cross-commit (D5)).
  - Google-style docstrings throughout.
- [x] 2.3 `__init__.py` — re-export the new public API (`build_kinematic_grid, compute_reynolds, derive_run_duration, select_holdout, render_inputs, generate_sweep`) and extend `__all__`.
- [x] 2.4 Run `uv run pytest tests/test_force_surrogate_sweep.py` — expect GREEN.

## 3. Committed artifacts (driver + the 27-config corpus; commit 2)

- [x] 3.1 Add `.gitattributes` rule `examples/**/inputs.3d.* text eol=lf` to make the LF pin **explicit** for the extension-less IAMReX decks (they are already LF today via the catch-all `* text=auto`; this removes the heuristic dependence — D6). Verified: committing the rule is a no-op on the pre-existing `flapping_wing`/`heaving_ellipsoid` decks' bytes (already LF in the index), so no `git add --renormalize` of existing files is required.
- [x] 3.2 `examples/prelim_sweep/generate_sweep.py` — thin driver with an importable `main(argv=None)` (so it can be smoke-tested into `tmp_path`): resolve repo paths, parse `--output`/`--timestamp` (default the committed output dir + a fixed ISO timestamp), call `generate_sweep`, print a summary. All logic stays in the library.
- [x] 3.3 Generate + stage the corpus in this **safe sequence** (so files are committed under the LF rule): (1) stage `.gitattributes` (task 3.1) **first**; (2) run the driver `uv run python examples/prelim_sweep/generate_sweep.py` → writes `examples/prelim_sweep/inputs/` (27 files `inputs.3d.s{φ}_f{f*×100:03d}_p{α}`), `sweep_manifest.json`, `sweep_manifest.units.json`, `sweep_provenance.json`; (3) `git add examples/prelim_sweep/` then `git add --renormalize examples/prelim_sweep/` (belt-and-suspenders LF); (4) `git diff --cached --stat` and confirm all 27 input files are staged (not swallowed by an IAMReX-output `.gitignore`) **and** that no pre-existing deck (e.g. `inputs.3d.validation`) appears in the staged diff (would mean the `.gitattributes` glob is broader than intended).
- [x] 3.4 `examples/prelim_sweep/README.md` — document the sweep: Aedes anchoring (point to the `AEDES_*` constants in `force_surrogate/constants.py` and the roadmap "Verified source numbers" table for the Bomphrey 2017 citation rather than re-tabulating numbers); CC-7 = ν\* fixed (Re range, per-config Re in `sweep_manifest.json`); N=2 wingbeats; force-only `plot_int=-1`; holdout policy; how to regenerate; that PR3 runs these. One-line note distinguishing this Aedes 39°-anchored sweep from the validated 70° demo.
- [x] 3.5 Record the **CC-7 resolution** in `docs/force_surrogate/roadmap.md`: (a) change the CC-7 heading (line ~98) `### CC-7. Reynolds handling in the sweep (decide in PR2).` → `… (RESOLVED in PR2: hold ν\* fixed).` and append a one-line resolution (policy (a); Re ≈ 43–90 deterministic in φ,f\*; per-config Re recorded; rationale → change `design.md` D1); (b) update the now tense-inconsistent sibling reference in the PR-split table row #2 (line ~152) from `document the Re policy (CC-7)` → `resolve + document the Re policy (CC-7 → ν\* fixed)`. **Do NOT tick the row-#2 status checkbox** (that is the merge-time `cleanup-merged` action, §5.4).
- [x] 3.6 Widen the `.github/workflows/ci.yml` lint job from `src/` to `src/ tests/ examples/prelim_sweep/` (both `ruff check` and `ruff format --check`). Lands in commit 2 (the path `examples/prelim_sweep/` must exist). Verify `uv run ruff check src/ tests/ examples/prelim_sweep/` and `uv run ruff format --check src/ tests/ examples/prelim_sweep/` are clean before pushing. (D9 — scoped; the rest of `examples/` is deferred to 7.2.)

## 4. Artifact tests (depend on §3 committed files; commit 2)

- [x] 4.1 `test_committed_sweep_matches_regeneration` — load `examples/prelim_sweep/sweep_manifest.json`; regenerate into `tmp_path` with the manifest's recorded `holdout.seed` and `provenance.generated_at`; assert each of the 27 committed input files **and** the committed `sweep_manifest.json` + `sweep_manifest.units.json` are byte-identical to their regenerated counterparts. (Scenario: "Byte-identical regeneration" against the committed corpus.)
- [x] 4.2 `test_committed_manifest_shape` — committed manifest has 27 configs, 6 holdout / 21 train, `reynolds_policy=="nu_star_fixed"`; `read_units_sidecar` accepts the committed units sidecar.
- [x] 4.3 `test_driver_smoke` — import the driver's `main` and call `main(["--output", str(tmp_path), "--timestamp", "2020-01-01T00:00:00+00:00"])`; assert it returns 0 and writes 27 inputs + manifest + units sidecar into `tmp_path`. Hermetic, cluster-free, no network (guards driver path/argv handling).

## 5. Validation

- [x] 5.1 `uv run ruff format --check src tests examples/prelim_sweep && uv run ruff check src tests examples/prelim_sweep` pass (line-length per pyproject = 88; google docstrings). *(After task 3.6, CI also enforces this exact scope — `src/ tests/ examples/prelim_sweep/`; the rest of `examples/` remains local-only pending the 7.2 cleanup.)*
- [x] 5.2 `uv run pytest --cov=mosquito_cfd.force_surrogate --cov-report=term-missing` — full suite passes; report coverage (target: `sweep.py` ≥ 95%, with the `render_inputs` missing-key + prefix paths and the `select_holdout`/config-validation error paths covered).
- [x] 5.3 `openspec validate add-force-surrogate-sweep-config --strict` passes.
- [ ] 5.4 On merge (NOT in PR2): tick `docs/force_surrogate/roadmap.md` row #2 **status checkbox** and `openspec archive add-force-surrogate-sweep-config` (via the `cleanup-merged` command).

## 6. Commit plan (every pushed commit GREEN)

**Merge method: squash-merge to `main`; the 3 conventional commits below are preserved as bullets in
the squash-commit body (matches PR #2 / commit `b820722`). Every commit pushed to the PR branch must
be CI-green on its own — `pull_request` CI runs the full `pytest -v` suite (only exit-5 tolerated) and
lints `src/`. Do red/green locally.**

1. `feat(force-surrogate): kinematic sweep generator (grid, Reynolds policy, wingbeat-scaled duration, seeded non-corner holdout, force-only inputs, manifest+units) + R_MID constant + tests`
   - Files: `src/mosquito_cfd/force_surrogate/{sweep.py,constants.py,__init__.py}`, `tests/test_force_surrogate_sweep.py` (§1 tests 1.1–1.22 + §2 impl together — GREEN against fixtures).
   - **Hard rule: the test file must NOT reference `examples/prelim_sweep/`** — §4 artifact tests are deferred to commit 2. CI after: **GREEN** (cluster-free, no corpus dependency).
   - **Lint-trap warning:** commit-1 CI lints `src/` only, so the new test file is *not* linted until commit 2 widens lint to `tests/` — a ruff violation here turns **commit 2** red, not commit 1. Before pushing commit 1, run `uv run ruff check tests/ && uv run ruff format --check tests/` regardless; do not treat commit-1 CI green as proof the test file is clean.
2. `feat(prelim-sweep): committed 27-config Aedes sweep corpus (driver + inputs + manifest + units sidecar + README) + artifact tests`
   - Files: `.gitattributes` (LF rule), `.github/workflows/ci.yml` (widen lint scope, task 3.6), `examples/prelim_sweep/{generate_sweep.py,README.md,inputs/inputs.3d.*}` (27), `sweep_manifest.json`, `sweep_manifest.units.json`, `docs/force_surrogate/roadmap.md` (CC-7 resolution text only), and the §4 artifact tests **appended to** `tests/test_force_surrogate_sweep.py`.
   - **Depends on commit 1** (driver + §4 tests import `force_surrogate.sweep`); **never cherry-pick/reorder/revert independently of commit 1.** CI after: **GREEN** (corpus present; byte-identity passes).
3. `docs(force-surrogate): spec for add-force-surrogate-sweep-config (CC-7 resolution)`
   - Files: the change's `specs/` + ticked `tasks.md`. **No roadmap status-checkbox edit** (row-#2 tick deferred to `cleanup-merged`, §5.4). CI after: **GREEN** (docs only).

Co-Authored-By: Claude Opus 4.8 (1M context) trailer on each (matches repo history).

## 7. Implementation deviations + named follow-ups (reconciliation)

- [x] 7.1 **Deviation: provenance split into `sweep_provenance.json` (was an in-manifest `provenance` block).** While wiring the byte-identity test it became clear that an in-manifest `git_commit` is non-reproducible across checkouts (the corpus is authored at one SHA; CI regenerates at another) and would defeat `test_committed_sweep_matches_regeneration`. Provenance (git/timestamp/base-hash) was moved to a separate `sweep_provenance.json`, leaving `sweep_manifest.json` a purely deterministic config description. `proposal.md`, `design.md` (D5 "Why a separate provenance file…", D7), and `spec.md` (manifest+provenance requirement, provenance scenario) were updated to match. No other deviations: base-file keys, holdout indices (`[1,11,14,17,19,25]` at seed 20260609), and all pinned numerics matched the plan.
- [ ] 7.2 **Deferred follow-up (not this PR):** (a) the `examples/flapping_wing/RESULTS.md` 70°-vs-39° clarification one-liner (roadmap calls it nice-to-have); (b) the stale `openspec/project.md` `src/` directory tree (missing `force_surrogate/`, Track B); (c) the **full `examples/` lint cleanup** — `ruff check examples/` reports 32 errors + 7 unformatted files (3 `flapping_wing/`, 2 `flow_past_sphere/`, 2 `heaving_ellipsoid/`); once fixed, CI lint can widen from `examples/prelim_sweep/` to all of `examples/`; (d) optionally normalize the published `openspec/specs/force-surrogate/spec.md` Given/When/Then to the bulleted style this delta uses (the published PR1 requirements use bare-bold clauses — a cosmetic cross-spec divergence that surfaces at archive). Knock these out as a tiny separate docs/lint PR (independent of the Track B roadmap).
